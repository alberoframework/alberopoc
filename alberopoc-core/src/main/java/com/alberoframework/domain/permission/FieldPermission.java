package com.alberoframework.domain.permission;

public enum FieldPermission {
	
	/* NONE, */ READ, WRITE
	
	//None shouldnt be explicit, if the user has no permissions then
	//its best that the variable containing that permission be empty
	
}
